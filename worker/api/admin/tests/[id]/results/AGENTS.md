# worker/api/admin/tests/[id]/results/ — Result Image Route Group

- Groups the per-MBTI result image route; the handler is in `[mbti]/image.ts`.
- No direct files here. Keep the route shape aligned with `worker/http/routes.ts` (`.../results/:mbti/image`).
