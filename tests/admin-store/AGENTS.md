# tests/admin-store/ — Admin Store Wrappers

- Covers frontend admin `store.js` wrappers (`store-wrappers`): dispatch / getState / subscribe over `reducer.js`.
- Pure/in-memory, no network. Assert state transitions, not DOM.

Run: `npm test`.
