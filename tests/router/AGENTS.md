# tests/router/ — Tiered Cache Getter Tests

- Covers route-descriptor cache options (`get-tiered-cache`): correct `cacheTtl` / `cacheTags` per route, `null` for non-cacheable routes.
- Keep distinct from `tests/routing/` (path matching/registry).

Run: `npm test`.
