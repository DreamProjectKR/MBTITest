# tests/frontend/ — Admin Reducer/Store Tests

- Covers admin `reducer.js` + `store.js` state transitions and branches (`admin-store`, `admin-reducer-branches`).
- Pure state logic; assert next-state per action. Render/DOM tests live in `tests/public/`.

Run: `npm test`.
