# tests/routing/ — Route Registry Tests

- Covers `matchRoute` / `routeDescriptors` (`route-registry`, `routes-branches`): path parsing, param extraction, method maps, registration drift.
- Add a case here whenever a route is added in `worker/http/routes.ts`.

Run: `npm test`.
