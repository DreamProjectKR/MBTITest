# Local dev should not depend on files inside `public/assets`.
# We proxy `/assets/*` via Pages Functions -> R2 binding.
# When local R2 is empty, the proxy falls back to this *public* base URL.
#
# NOTE: The `*.r2.cloudflarestorage.com/<bucket>` endpoint is NOT publicly readable
# without signing; use your public R2 dev URL (`pub-...r2.dev`) or a custom domain.
R2_PUBLIC_BASE_URL="https://pub-9394623df95a4f669f145a4ede63d588.r2.dev"